#include "connexion.hpp" 
#include <cmath>
#include <cstdlib> 


/*Connexion::Connexion(double d = 1.5, double w = 0.1)
{
	if((d == 0) or (w == 0))
	{
		random_connexion(); 
	} 
	
} 

Connexion::~Connexion(){}
s
void Connexion::random_connexion() 
{
	delay_ = (rand() % 10) + 1; 
	do { 
		weight_ = ((rand()/RAND_MAX) * 10) - 5; 
	} while (weight_ == 0); 
	
}*/
